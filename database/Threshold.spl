SELECT 
    endpoint,
    COUNT(*) AS total_requests,
    ROUND(AVG(response_time_ms), 2) AS avg_latency_ms
FROM system_logs
WHERE created_at >= NOW() - INTERVAL 7 DAY
GROUP BY endpoint
HAVING avg_latency_ms > 200.00
ORDER BY avg_latency_ms DESC;
